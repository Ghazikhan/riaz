<p id="notice"><%= notice %></p>

<h1>Listing Sessions</h1>

<table>
  <thead>
    <tr>
      <th Session sdate> Session Start Date </th>
      <th Session edate > Session End Date</th>
      <th>Program</th>
      <th colspan="3"> Navigation</th>
    </tr>
  </thead>

  <tbody>
    <% @sessions.each do |session| %>
      <tr>
        <td><%= session.session_sdate  %></td>
        <td><%= session.session_edate %></td>
        <td><%= session.program.program_name %></td>
        <td><%= link_to 'Show', session %></td>
        <td><%= link_to 'Edit', edit_session_path(session) %></td>
        <td><%= link_to 'Destroy', session, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Session', new_session_path %>
<br><br><br><br>
<hr>
<div style="padding-left:45%; font-size:16px;">
| <%= link_to 'Home', pages_home_path %> |
</div>
<hr>
